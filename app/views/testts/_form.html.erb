<%= form_for(@testt) do |f| %>
  <% if @testt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testt.errors.count, "error") %> prohibited this testt from being saved:</h2>

      <ul>
      <% @testt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :current_user_id %><br>
    <%= f.number_field :current_user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
