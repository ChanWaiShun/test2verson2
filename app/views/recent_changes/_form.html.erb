<%= form_for(@recent_change) do |f| %>
  <% if @recent_change.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recent_change.errors.count, "error") %> prohibited this recent_change from being saved:</h2>

      <ul>
      <% @recent_change.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :version %><br>
    <%= f.text_field :version %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.text_field :date %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
