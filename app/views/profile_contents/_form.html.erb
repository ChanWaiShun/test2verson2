<%= form_for(@profile_content) do |f| %>
  <% if @profile_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_content.errors.count, "error") %> prohibited this profile_content from being saved:</h2>

      <ul>
      <% @profile_content.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
    <%= f.hidden_field :profile_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
