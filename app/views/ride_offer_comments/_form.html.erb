<%= form_for(@ride_offer_comment) do |f| %>
  <% if @ride_offer_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ride_offer_comment.errors.count, "error") %> prohibited this ride_offer_comment from being saved:</h2>

      <ul>
      <% @ride_offer_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_name %><br>
    <%= f.text_field :user_name %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
    <%= f.hidden_field :ride_offer_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
