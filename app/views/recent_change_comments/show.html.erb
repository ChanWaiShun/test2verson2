<p id="notice"><%= notice %></p>

<p>
  <strong>User name:</strong>
  <%= @recent_change_comment.user_name %>
</p>

<p>
  <strong>Body:</strong>
  <%= @recent_change_comment.body %>
</p>

<p>
  <strong>Recent change id:</strong>
  <%= @recent_change_comment.recent_change_id %>
</p>

<%= link_to 'Edit', edit_recent_change_comment_path(@recent_change_comment) %> |
<%= link_to 'Back', recent_changes_path %>

