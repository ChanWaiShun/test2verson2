<%= form_for(@recent_change_comment) do |f| %>
  <% if @recent_change_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recent_change_comment.errors.count, "error") %> prohibited this recent_change_comment from being saved:</h2>

      <ul>
      <% @recent_change_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_name %><br>
    <%= f.text_field :user_name %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.hidden_field :recent_change_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
